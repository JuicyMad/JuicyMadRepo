<h1>Available products</h1>
<ul>
    {{#each products as |product|}}
    <li>
        <a href="/products/{{product.id}}/edit">
        {{product.name}} / {{product.propierties}} propierties / {{product.ingredients}} ingredients / {{product.prices}} prices / {{product.images}} images
        ></a>
        <form action="/products/{{product.id}}"/select method="POST"
        <button><select name="" id=""></select></button>
    </form>
    </li>
</ul>