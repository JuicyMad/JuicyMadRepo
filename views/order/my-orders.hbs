<div class="content">
    <h1>Lista de pedidos</h1>
  <div class="order ">
    {{#each userOrders as |order|}}
    <div class="order-card">
      <p>{{order.status}}</p>


      {{#each order.distinctProducts as |product|}}
     <div class="product-order">
       <h6>{{product.name}} ×  {{product.count}}</h6>
       <div class="order-img">
      <img src="{{product.img}}" alt="{{product.img}}">

       </div>
     </div>
      
      {{/each}}
    </div>
    {{/each}}
  </div>
</div>
      {{!-- <select name="select">
        <option value="value1">o</option>
        <option value="value2" selected>{{order.status}}</option>
        <option value="value3">Value 3</option>
      </select> --}}